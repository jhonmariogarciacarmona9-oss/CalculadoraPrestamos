# Base de datos — usar variables de entorno en producción
# En Railway/Render configura estas variables en el dashboard
spring.datasource.url=${DATABASE_URL:jdbc:postgresql://aws-0-eu-west-1.pooler.supabase.com:5432/postgres?sslmode=require&sslfactory=org.postgresql.ssl.NonValidatingFactory&prepareThreshold=0}
spring.datasource.username=${DB_USER:postgres.rhfmkfetcstneqdhhegj}
spring.datasource.password=${DB_PASSWORD:Marito199723gc}
spring.datasource.driver-class-name=org.postgresql.Driver

# HikariCP - pool de conexiones con soporte SSL Supabase
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.maximum-pool-size=5
spring.datasource.hikari.minimum-idle=1
spring.datasource.hikari.idle-timeout=300000
spring.datasource.hikari.keepalive-time=60000
spring.datasource.hikari.connection-test-query=SELECT 1

# Servidor — Railway inyecta PORT automáticamente
server.port=${PORT:8080}

# Thymeleaf
spring.thymeleaf.cache=false
spring.thymeleaf.encoding=UTF-8

# Logs
logging.level.org.springframework.security=WARN
logging.level.com.calculadora=INFO
