# ========================================
# Sistema de Préstamos — Railway / Render
# ========================================

# Plataforma de build
[build]
builder = "NIXPACKS"

# Variables de entorno mínimas a configurar en el dashboard:
#   DATABASE_URL  → URL completa de Supabase (con ?sslmode=require)
#   DB_USER       → postgres
#   DB_PASSWORD   → tu contraseña
#   PORT          → lo inyecta Railway automáticamente
